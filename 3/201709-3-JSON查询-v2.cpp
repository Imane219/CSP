#include <iostream>
#include <string>
#include <unordered_map>
#include <algorithm>
using namespace std;

unordered_map<string, string> ans;

string read()
{
  char c = getchar();
  if(c == '{')
    return "{";
  else if(c == '}')
    return "}";
  else if(c == '"')

}